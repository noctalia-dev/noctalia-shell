--- a/src/libprojectM/Renderer/TextureSamplerDescriptor.cpp
+++ b/src/libprojectM/Renderer/TextureSamplerDescriptor.cpp
@@ -21,7 +21,9 @@ TextureSamplerDescriptor::TextureSamplerDescriptor(const std::shared_ptr<class T

 auto TextureSamplerDescriptor::Empty() const -> bool
 {
-    return m_texture->Empty();
+    // m_texture can be null when GetRandomTexture() finds no texture files
+    // (no texture search paths configured, or prefix filter matches nothing).
+    return !m_texture || m_texture->Empty();
 }

 void TextureSamplerDescriptor::Bind(GLint unit, const Shader& shader) const
